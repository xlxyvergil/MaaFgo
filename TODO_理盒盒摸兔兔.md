# 理盒盒 / 摸兔兔 后续开发清单

> 状态更新：2026-08-15
> 当前阶段：**理盒盒 P0（一键领取 + 按数量保留）代码已完成，导航/筛选流程已调通**
> 说明：本清单用于逐项过流程，每完成一项勾掉 `[ ]`。

---

## 一、理盒盒 P0 收尾（先过这部分）

### 1.1 星级取色区坐标校准（最优先，阻塞后续）

一键领取、按数量保留都要先「选择星级」，而 `STAR_ROI`（3/4/5 星取色区）、`STAR_TAP`（点击位）目前还是 1920×2/3 纯估算，**未实测过**，大概率不准。

- [ ] 跑一轮「保留五星」，看日志 `[整理礼物盒] 星级选中态 ...` 是否输出 `[False, False, True]`
- [ ] 若不对，微调 `agent/custom/box_action.py` 里的：
  - `STAR_ROI = [(580,467,33,33), (393,467,33,33), (205,467,33,33)]`（3/4/5 星取色区）
  - `STAR_TAP = [(640,483), (453,483), (265,483)]`（3/4/5 星点击位）

### 1.2 数字 OCR 识别率（按数量保留的核心）

- [ ] 跑「按数量保留」，看日志 `数字识别失败` 的比例
- [ ] 若识别率低，两个方向：① 调 `NUM_ROI_*` 偏移（当前 `179,-51,79,38`）；② 回退移植 BBchannel 的数字模板匹配 `checkNums`（整图 + part0-9 半图二次区分）

### 1.3 勾选框偏移验证

- [ ] 跑「按数量保留」，确认勾选框能点中（用户已给 `667,29,60,60` 检测 / `697,29` 点击，待实测）

### 1.4 一键领取完整流程

- [ ] 只开「一键领取 3/4/5 星」跑一轮：筛选 → 循环领空 → 识别「全部领取按钮已领完」结束
- [ ] 礼物盒满时走「礼物盒满结束」分支

### 1.5 补 2 张截图（增强稳定性，非阻塞）

当前 `点礼物盒图标`、`筛点筛选按钮` 是固定坐标点击顶着，补图后改模板匹配更稳。

- [ ] 礼物盒入口图标（游戏里 菜单 → 商店，商店界面左下角「礼物盒」按钮）
- [ ] 筛选按钮（礼物盒界面右上角）
- [ ] 补图后：pipeline 里 `整理礼物盒-点礼物盒图标`、`整理礼物盒-筛点筛选按钮` 从 `Click target` 改为 `TemplateMatch + Click`

---

## 二、理盒盒 P1（扩展）

### 2.1 贩卖狗粮（P1a，代码已完成，待实测）

- [x] 模板图准备（20 张/服 + 3 张共用，中文命名）
- [x] 导航到灵基变还 pipeline（7 节点）
- [x] 筛选狗粮+星级 + 贩卖循环（长按→红框→变还→销毁→QP满→关闭→到底）
- [x] option 贩卖 3/4/5 星 + QP 满自动处理开关
- [ ] **实测校准贩卖坐标**（最优先，全是 1920×2/3 估算）：筛选按钮 (981,128)、狗粮类型 (640,100)、3/4/5星按钮 (643/457/270,245)、卡片1 (136,248)、变还决定 (1153,671)、销毁 (843,628)、关闭 (640,628)、QP确认 (920,560)
- [ ] 验证「到底判断」（第一张卡片锁定 → 结束）
- [ ] 验证 QP 满处理（自动点掉 / 停止）

### 2.2 保管室（P1b，未开始）

- [ ] **保管室**（`storeDF`）—— 复用 `expball.sell(storeMode=True)`，导航到保管室存入，复用 P1a 的筛选/宫格遍历，只差 `doStore` 动作
- [ ] **自动喂从者**（`levelupServant`）—— 依赖 expball 升级逻辑，建议最后做或砍掉

---

## 三、摸兔兔（无限池抽奖，原始核心功能，未开始）

> 「摸兔兔」名字来源，即无限池兑奖抽奖。

- [ ] 模板图准备：`unlimitedPool/CH/` + `unlimitedPool/JP/` 的 draw 相关图（`in_draw`、`draw10/100`、`ge`、`pool_empty`、`reset`、`do_reset`、`x`、`pay`、`choose_pay`）按 2/3 缩到 720p，中文命名，CH→cn、JP→base
- [ ] 导航到无限池兑奖界面（主界面 → 活动报酬 → 兑奖界面）
- [ ] 抽奖主循环：`gotoDraw()` → 循环 `checkPoolEmpty()` 判空 → 点抽奖(×5 或长按) → 关结果弹窗 → 有 `reset` 重置奖池继续，无则「兔兔摸完了」结束
- [ ] pipeline（导航 + 简单连点）+ custom action（奖池空判断、重置、结束条件）

---

## 四、收尾工程

- [ ] 同步到打包版（**等用户明确说「同步」再做**，遵循记忆里的同步规范：diff 对比 → 备份 → 增量覆盖 → 校验）
- [ ] 写更新公告（UPDATE_*.md，面向使用者风格：写「能做什么」，不写实现细节）
